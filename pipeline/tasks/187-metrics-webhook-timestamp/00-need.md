# Görev 187 — İhtiyaç

SPEC 064 `metrics --alert-webhook` payload'ına `timestamp` (SPEC 180/186 kardeşi).

## Kabul
- Payload'a `timestamp: str (ISO 8601 seconds)` alan-ekleme (SPEC 032.4).
- Mevcut alanlar DOKUNULMADI.
- SPEC 169 `alert_window_minutes` yolu AYNI (bit-uyumlu).
