# Görev 201 — İhtiyaç

atlas-doctor.yml webhook payload'a `event` alanı (SPEC 141 kardeşi).

## Kabul
- Payload heredoc'una `"event":"${{ github.event_name }}"`.
- Mevcut 7 alan DOKUNULMADI (SPEC 135 6 + SPEC 185 strict = 7).
- Step adı SPEC 201 referansı.
