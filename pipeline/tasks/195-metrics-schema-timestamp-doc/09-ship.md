# Görev 195 — Teslim

`metrics --schema` alert_payload'a `timestamp` alan-ekleme (SPEC 187 belge).

## Uygulama
- alert_payload sonuna 14. alan `timestamp` (spec=187, when=webhook only).
- notes'a SPEC 187 + SPEC 195 satırları eklendi.

## Kanıt
- +3 test; metrics_schema regresyon 39 yeşil.
