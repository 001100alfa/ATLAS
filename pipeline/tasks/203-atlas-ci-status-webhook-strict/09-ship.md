# Görev 203 — Teslim

atlas-ci-status.yml webhook payload'a `check` alanı (SPEC 185 kalıp).

## Uygulama
- Payload heredoc'una `"check":"readme-badge"` (kontrol tipi belge).
- Mevcut 6 alan DOKUNULMADI; 6 → 7.
- Step adı SPEC 203 referansı.

## Kanıt
- +3 test SPEC 203; workflow test 141 yeşil.
