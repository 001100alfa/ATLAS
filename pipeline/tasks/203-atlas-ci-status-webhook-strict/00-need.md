# Görev 203 — İhtiyaç

atlas-ci-status.yml webhook payload'a `check` alanı (SPEC 185 doctor
strict kalıbı — sabit değer belge).

## Kabul
- Payload heredoc'una `"check":"readme-badge"`.
- Mevcut 6 alan DOKUNULMADI (SPEC 141 5 + SPEC 191 timestamp).
- Step adı SPEC 203 referansı.
