# Görev 123 — İhtiyaç

SPEC 104+110+114 zinciri (doctor `--diff-history-all --format prometheus
--out --gzip`) tam birlikte regresyon test yok. Salt-test tur (SPEC
116/121/122 kalıbı).

## Kabul

- Yeni `tests/test_cli_doctor_full_chain.py`.
- Kod DEĞİŞMEZ; tam zincir + `--strict` ortogonalliği.
